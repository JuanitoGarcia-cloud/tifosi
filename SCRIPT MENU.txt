CREATE TABLE menu (
	id_menu INT NOT NULL AUTO_INCREMENT,
    	nom VARCHAR(50) NOT NULL,
    	prix DECIMAL(5,2) NOT NULL,
    	PRIMARY KEY(id_menu)
);
ALTER TABLE `menu` CHANGE `nom` `nom_menu` VARCHAR(50) CHARACTER SET utf8mb4 COLLATE utf8mb4_bin NOT NULL;